.page-header
  = link_to new_ingredient_path, class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-plus
    New Ingredient
  %h1 Listing ingredients

.table-responsive
  %table.table.table-striped.table-bordered.table-hover
    %thead
      %tr
        %th Meat
        %th Vegetable
        %th Seasoning
        %th Grain
        %th Fruit
        %th Recipe id
        %th{colspan: 3}

    %tbody
      - @ingredients.each do |ingredient|
        %tr
          %td= ingredient.meat
          %td= ingredient.vegetable
          %td= ingredient.seasoning
          %td= ingredient.grain
          %td= ingredient.fruit
          %td= ingredient.recipe_id
          %td
            = link_to ingredient do
              %span.glyphicon.glyphicon-ok
          %td
            = link_to edit_ingredient_path(ingredient) do
              %span.glyphicon.glyphicon-pencil
          %td
            = link_to ingredient, data: { confirm: 'Are you sure?' }, method: :delete do
              %span.glyphicon.glyphicon-remove
